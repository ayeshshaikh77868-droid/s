PART 16 — COMPLETE PRACTICAL 4 COMMAND SHEET
Kali
whoami
hostname
ip addr
sudo apt update
SEToolkit
setoolkit

If required:

sudo apt install set
SET flow
SEToolkit
   ↓
Social-Engineering Attacks
   ↓
Website Attack Vectors
   ↓
Controlled local/dummy demonstration
PART 17 — WHAT EACH COMMAND DOES
Command	Purpose
whoami	Shows current user
hostname	Shows computer name
ip addr	Shows network interfaces/IP addresses
sudo	Executes command with elevated privileges
apt update	Updates package information
apt install set	Installs SET package if available
setoolkit	Starts Social Engineering Toolkit
